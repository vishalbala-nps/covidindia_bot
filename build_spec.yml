version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "Hello Bala, This is a test variable!!!"

steps:
  - type: Command
    name: "Sleep"
    command: |
      sleep 10
  - type: Command
    name: "Build Source"
    command: |
      echo $testEnv > covidindia_bot.txt
artifacts:
  - name: result_txt
    type: BINARY
    files:
      - covidindia_bot.txt
